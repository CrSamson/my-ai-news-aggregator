# Scrapers
feedparser==6.0.12
requests==2.32.5
# Article body extraction (URL -> markdown). Lightweight (~5 MB of deps)
# replacement for Docling. See tools/compare_extractors.py for the A/B that
# justified the swap. Per-source fetch_content config decides where to enable.
trafilatura>=2.0,<3.0

# Validation
pydantic>=2.0,<3.0

# Database
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9

# Config
python-dotenv>=1.0

# LLM summarization
openai>=1.50.0

# Scheduler
apscheduler>=3.10.0
