# Scrapers
feedparser==6.0.12
requests==2.32.5
# Article body extraction (URL -> markdown). Lightweight (~5 MB of deps)
# replacement for Docling. See tools/compare_extractors.py for the A/B that
# justified the swap. Per-source fetch_content config decides where to enable.
trafilatura>=2.0,<3.0

# Validation
pydantic>=2.0,<3.0

# Database
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9
pgvector>=0.3,<1                    # Vector type for SQLAlchemy + pgvector ext

# Config
python-dotenv>=1.0

# LLM summarization
openai>=1.50.0

# Embeddings (default: local MiniLM, see agent/embedder.py)
sentence-transformers>=3.0,<6
numpy>=1.26,<3
scikit-learn>=1.4,<2

# Scheduler
apscheduler>=3.10.0
