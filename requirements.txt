# Scrapers
feedparser==6.0.12
requests==2.32.5
# Article body extraction (URL -> markdown). Lightweight (~5 MB of deps)
# replacement for Docling. See tools/compare_extractors.py for the A/B that
# justified the swap. Per-source fetch_content config decides where to enable.
trafilatura>=2.0,<3.0

# Validation
pydantic>=2.0,<3.0

# Database
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9
pgvector>=0.3,<1                    # Vector type for SQLAlchemy + pgvector ext

# Config
python-dotenv>=1.0

# LLM summarization
openai>=1.50.0

# Embeddings (default: OpenAI text-embedding-3-small, see agent/embedder.py).
# Numpy is also needed by the Phase 4 clusterer. The MiniLM fallback path
# additionally needs `pip install sentence-transformers`; not pinned here
# because the default doesn't require it.
numpy>=1.26,<3

# Scheduler
apscheduler>=3.10.0
