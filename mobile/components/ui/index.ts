/**
 * components/ui — design-system primitive barrel export.
 *
 * Components import from this file rather than from individual modules,
 * so a future reorganisation (e.g. extracting to a separate package) is
 * a one-file change.
 */
export { Card } from "./Card";
export { TopicChip, TopicChipRow } from "./TopicChip";
export { SourceDot, SourceDotRow } from "./SourceDot";
export { DisplayHL, Headline, Body, Preview, Meta, SectionLabel } from "./Text";
